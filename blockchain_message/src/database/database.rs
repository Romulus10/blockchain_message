pub struct Database {
    contacts: Vec<Contact>,
    messages: Vec<Message>
}

impl Database {
    pub fn insert(Message msg) -> i32 {}
    pub fn delete(i32 msgid) -> i32 {}
    pub fn read(i32 msgid) -> message {}
}
